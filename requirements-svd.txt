# Stable Video Diffusion — installer dans un venv dédié (recommandé).
#
# 1) PyTorch avec CUDA (choisir la commande sur https://pytorch.org/get-started/locally/)
#    Exemple Windows + CUDA 12.4 :
#    pip install torch torchvision --index-url https://download.pytorch.org/whl/cu124
#
# 2) Puis :
#    pip install -r requirements-svd.txt
#
# Première exécution : téléchargement du modèle (~plusieurs Go) via Hugging Face.

diffusers>=0.27.0
transformers>=4.38.0
accelerate>=0.27.0
safetensors>=0.4.0
pillow>=10.0.0
numpy>=1.24.0
imageio[ffmpeg]>=2.34.0
huggingface-hub>=0.21.0

# Optionnel : attention mémoire GPU (si installable sur votre CUDA)
# xformers
